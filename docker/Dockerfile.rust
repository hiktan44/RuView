# WiFi-DensePose Rust Sensing Server
# Includes RuVector signal intelligence crates
# Multi-stage build for minimal final image

# Stage 1: Build
FROM rust:1.85-bookworm AS builder

WORKDIR /build

# Build dependencies for ndarray-linalg. We link the SYSTEM OpenBLAS
# (libopenblas-dev) rather than compiling OpenBLAS from source — building it in
# `cargo build` needs several GB of RAM and OOM-kills the deploy server (cargo
# exit 101). gfortran is still needed for the LAPACK symbols.
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential gfortran perl make pkg-config cmake \
    libopenblas-dev \
    && rm -rf /var/lib/apt/lists/*

# Copy workspace files
COPY rust-port/wifi-densepose-rs/Cargo.toml rust-port/wifi-densepose-rs/Cargo.lock ./
COPY rust-port/wifi-densepose-rs/crates/ ./crates/

# Copy vendored RuVector crates
COPY vendor/ruvector/ /build/vendor/ruvector/

# Build release binary.
# The shared deploy server has limited RAM and runs 2 concurrent builds, so a
# normal parallel release build OOM-kills rustc (cargo exits 101 after minutes
# of compiling — even though it builds fine locally). Constrain memory use:
#   CARGO_BUILD_JOBS=1        — compile one crate at a time (biggest RAM saver)
#   codegen-units=1 + opt for size-ish — fewer parallel codegen threads per crate
# This is slower but fits in the build host's memory budget.
ENV CARGO_BUILD_JOBS=1
ENV CARGO_PROFILE_RELEASE_CODEGEN_UNITS=1
ENV CARGO_PROFILE_RELEASE_LTO=off
RUN cargo build --release -p wifi-densepose-sensing-server 2>&1 \
    && strip target/release/sensing-server

# Stage 2: Runtime
FROM debian:bookworm-slim

# Runtime needs the shared OpenBLAS + libgfortran the binary now links against
# dynamically (openblas-system), plus curl for the compose healthcheck.
RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates curl \
    libopenblas0 libgfortran5 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Copy binary
COPY --from=builder /build/target/release/sensing-server /app/sensing-server

# Copy UI assets
COPY ui/ /app/ui/

# HTTP API
EXPOSE 3000
# WebSocket
EXPOSE 3001
# ESP32 UDP
EXPOSE 5005/udp

ENV RUST_LOG=info

# Bind to all interfaces so the reverse proxy (Coolify/Traefik) can reach the
# container. Default is 127.0.0.1 which is unreachable from outside the container.
ENV SENSING_BIND_ADDR=0.0.0.0

# CSI_SOURCE controls which data source the sensing server uses at startup.
# auto      — probe UDP port 5005 for an ESP32 first; fall back to simulation (default)
# esp32     — receive real CSI frames from an ESP32 device over UDP port 5005
# wifi      — use host Wi-Fi RSSI/scan data (Windows netsh; not available in containers)
# simulated — generate synthetic CSI frames (no hardware required)
# Override at runtime:  docker run -e CSI_SOURCE=esp32 ...
ENV CSI_SOURCE=auto

ENTRYPOINT ["/bin/sh", "-c"]
# Shell-form CMD allows $CSI_SOURCE to be substituted at container start.
# The ENV default above (CSI_SOURCE=auto) applies when the variable is unset.
CMD ["/app/sensing-server --source ${CSI_SOURCE} --tick-ms 100 --ui-path /app/ui --http-port 3000 --ws-port 3001"]
